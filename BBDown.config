#本文件是BBDown程序的配置文件
#以#开头的都会被程序忽略
#然后剩余非空白内容程序逐行读取，对于一个选项，其参数应当在下一行出现

#下面将设置输出文件名格式，为避免文件名冲突，将使用ownerMid__ownerName作为文件夹名，videoTitle作为文件名
#可自行根据需要修改
--file-pattern
<videoTitle>__<bvid>__<videoDate>

-c
# 此处放置从浏览器中获取到的cookie（使用获取到cookie替换此行）

